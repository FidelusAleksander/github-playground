name: "CodeQL config"
queries: 
  - uses: security-extended
  - uses: ./queries
paths-ignore:
  - tests
  - lib
disable-default-queries: true
# query-filters:
#  - exclude:
#      id: js/code-injection
